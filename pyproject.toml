[project]
name = "workbenchrl"
version = "0.1.0"
description = "Multi-task RL environment"
authors = [{name = "Arnab"}]

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openai",
    "openenv>=0.1.0"
]

# ✅ IMPORTANT CHANGE
[project.scripts]
server = "server.app:main"

[tool.openenv]
entrypoint = "server.app:main"
