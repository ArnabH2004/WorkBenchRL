[project]
name = "workbenchrl"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openai",
    "openenv>=0.1.0"
]

[project.scripts]
server = "inference:main"
start = "inference:main"

[tool.openenv]
entrypoint = "inference:app"
